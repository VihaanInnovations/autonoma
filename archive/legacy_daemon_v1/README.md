This directory contains experimental daemon architecture
from earlier Autonoma prototypes.

It is not part of the current community edition CLI
and is kept for historical reference only.

The active implementation lives in src/autonoma.